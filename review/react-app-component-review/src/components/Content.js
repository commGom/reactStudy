import React from 'react';

const Content = () => {
    return (
        <div>
            <h2>HTML</h2>
            HTML is HyperText Markup Language.
        </div>
    );
};

export default Content;